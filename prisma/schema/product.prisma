model Product {
    id Int @id @default(autoincrement())
    name String
    description String
    price Float
    quantity Int
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
    tags Tag[]
    reviews Review[]
    images ProductImage[]
    categories Category[]
}