model Customer {
    id Int @id @default(autoincrement())
    name String
    email String
    password String
    registrationDate DateTime @default(now())
    updatedAt DateTime @updatedAt
    reviews Review[]
    orders Order[]
    addresses Address[]
}